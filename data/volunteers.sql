CREATE TABLE njcovid.volunteers
(
    id integer NOT NULL DEFAULT nextval('njcovid.volunteers_id_seq'::regclass),
    submission_date character varying(30) COLLATE "default" NOT NULL,
    first_name character varying(255) COLLATE "default" NOT NULL,
    last_name character varying(255) COLLATE "default" NOT NULL,
    email character varying(255) COLLATE "default" NOT NULL,
    phone_number character varying(255) COLLATE "default" NOT NULL,
    address_1 character varying(255) COLLATE "default",
    address_2 character varying(255) COLLATE "default",
    city character varying(255) COLLATE "default",
    state character varying(40) COLLATE "default",
    postal character varying(10) COLLATE "default",
    country character varying(255) COLLATE "default" DEFAULT 'USA'::character varying,
    date_of_birth character varying(10) COLLATE "default" NOT NULL,
    practice_area character varying(255) COLLATE "default" NOT NULL,
    populations character varying(255) COLLATE "default" NOT NULL,
    specialties_and_certs text COLLATE "default",
    volunteer_license_status character varying(40) COLLATE "default" NOT NULL,
    volunteer_license_number character varying(40) COLLATE "default",
    npi_number character varying(40) COLLATE "default",
    other_state_license character varying(255) COLLATE "default",
    last_practice_date character varying(100) COLLATE "default",
    certifications character varying(40) COLLATE "default",
    locations character varying(255) COLLATE "default",
    telehealth character varying(20) COLLATE "default",
    medical_reserve character varying(20) COLLATE "default",
    languages character varying(255) COLLATE "default",
    available_date character varying(10) COLLATE "default",
    describe_interest text COLLATE "default",
    dca_license_status character varying(40) COLLATE "default",
    dca_license_profession character varying(255) COLLATE "default",
    dca_license_verified boolean NOT NULL DEFAULT false,
    background_check text COLLATE "default",
    overall_status character varying(255) COLLATE "default" NOT NULL DEFAULT 'pending'::character varying,
    reviewer character varying(255) COLLATE "default",
    staffing_agency_assigned character varying(255) COLLATE "default",
    facility_assigned character varying(255) COLLATE "default",
    role character varying(255) COLLATE "default",
    assignment_start_date character varying(20) COLLATE "default",
    assignment_end_date character varying(20) COLLATE "default",
    extra_info_request text COLLATE "default",
    in_person_ok character varying(255) COLLATE "default",
    covid19_work_ok character varying(255) COLLATE "default",
    experience_years smallint,
    practice_settings character varying(255) COLLATE "default",
    icu_experience character varying(255) COLLATE "default",
    student_years_in smallint,
    student_expected_grad_date character varying(30) COLLATE "default",
    weekly_available_hours character varying(255) COLLATE "default",
    geography_notes text COLLATE "default",
    volunteer_questions text COLLATE "default",
    comments text COLLATE "default",
    CONSTRAINT volunteers_pkey PRIMARY KEY (id)
);